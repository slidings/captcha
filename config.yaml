seed: 42
data:
  train_dir: "data/cleaned_train"
  # Note that the image height and width must be proportionate to most of training data
  img_height: 48
  max_width: 256 #192 seems to be good middle ground
  grayscale: false
  num_workers: 8

train:
  batch_size: 32
  epochs: 60
  lr: 1e-3
  weight_decay: 1e-4
  grad_clip: 5.0
  lr_decay: 0.95

model:
  cnn_out_channels: 256
  lstm_hidden: 384
  lstm_layers: 2
  dropout: 0.3

log:
  print_every: 50
  ckpt_dir: "checkpoints_colour"
